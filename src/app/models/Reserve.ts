import { ObjectLoader } from '../utils/objectloader';

export class reserve extends ObjectLoader {
  id: string;
  campname: string;
  reservename: string;
  start: Date;
  end: Date;
  tree: string;
  trash: string;
  appliance: string;
  concent: string;
  tool: string;
  lamp: string;
  vim: string;
  play: string;
  tent: string;
  request: string;
  people: string;
  createDT: string;
}
